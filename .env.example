# Skopiuj do .env i uzupełnij. .env jest w .gitignore — nie commituj.
ANTHROPIC_API_KEY=
PORT=3000
# ID konta Cloudflare — wymagane gdy masz >1 konto (inaczej wrangler nie wybierze).
# setup.sh ustawi je automatycznie; można też wpisać ręcznie (wrangler whoami pokazuje ID).
CLOUDFLARE_ACCOUNT_ID=
